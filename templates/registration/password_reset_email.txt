Someone asked to reset the password for the Inventory account "{{ user.get_username }}".

If that was you, open this link and choose a new one:

{{ protocol }}://{{ domain }}{% url 'password_reset_confirm' uidb64=uid token=token %}

The link works once and expires in a few hours.

If it wasn't you, ignore this email. Nothing has changed and your password
still works.
